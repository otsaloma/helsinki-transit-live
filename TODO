Helsinki Transit Live 0.2
=========================

 [X] Ship simple icons instead using ones from the web interface
 [X] Use text labels instead of icons for route names (this should
     missing route names for some newer lines)
 [X] Keep updating route label in case vehicle changes its route
 [X] Use labels "M" and "V" for metro instead of "1" and "2"

Helsinki Transit Live 0.3
=========================

 * Initialize center position from GPS
 * Show the current position with a marker?
